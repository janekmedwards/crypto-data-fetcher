services:
  - type: worker
    name: crypto-data-fetcher
    runtime: python
    buildCommand: ""
    startCommand: python real_time_fetcher.py
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: crypto-db
          property: connectionString

databases:
  - name: crypto-db
